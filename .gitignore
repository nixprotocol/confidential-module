# Protoc artifact: generated .pb.go files placed at the full module path
# instead of the local types/ directory. These are duplicates of types/*.pb.go.
github.com/

# Test artifacts
coverage.out

# Talk-prep build artifacts (reproducible via docs/talk-prep/generate_guide.py)
docs/talk-prep/confidential-module-qa-guide.html
docs/talk-prep/diagrams/*.png
