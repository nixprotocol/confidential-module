# Protoc artifact: generated .pb.go files placed at the full module path
# instead of the local types/ directory. These are duplicates of types/*.pb.go.
github.com/

# Test artifacts
coverage.out

# Internal working material -- kept locally, never published.
# Agent session state, brainstorm scratch, implementation plans/specs, and
# personal talk preparation. None of it is part of the module.
.superpowers/
docs/superpowers/
docs/talk-prep/
